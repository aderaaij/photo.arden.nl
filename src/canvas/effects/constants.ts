// Shared across all cover effects.

export const COVER_ASPECT = 2 / 3 // width : height — every cover is framed to 2:3
export const DEFAULT_BASE_WIDTH = 2.6 // plane width in world units

// Which effect loads on a fresh visit (before any localStorage choice).
export const DEFAULT_EFFECT_ID = 'bend'
